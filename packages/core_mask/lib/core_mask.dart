/// FEBRIC Mask Engine (ADR-0007).
///
/// Frozen mask architecture: kinds, boolean operations, refinements and
/// mask stacks. Immutable models only — mask *evaluation* is a rendering
/// concern of a later milestone.
///
/// Pure Dart. Depends only on `core_geometry`.
library;

export 'src/mask_model.dart';
