name: core_garment
description: "FEBRIC Garment Structure and Library engines: schemas, part definitions, templates, content packs, instantiation."
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  core_common:
    path: ../core_common
  core_document:
    path: ../core_document

dev_dependencies:
  test: ^1.25.0
