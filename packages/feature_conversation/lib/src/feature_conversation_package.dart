/// Package marker for `feature_conversation`.
///
/// Conversation AI surface: intent, target, constraint, plan, preview, approval.
///
/// Keeps the package importable and testable from day one; real
/// implementation arrives in its architecture milestone.
abstract final class FeatureConversationPackage {
  static const String id = 'feature_conversation';
  static const String description =
      'Conversation AI surface: intent, target, constraint, plan, preview, approval.';
}
