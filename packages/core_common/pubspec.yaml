name: core_common
description: "FEBRIC Shared kernel: result types, error model, logging, activity ledger, pure utilities."
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dev_dependencies:
  test: ^1.25.0
