name: feature_ai_assist
description: "FEBRIC Scoped AI assistance: generate, modify, replace, recolor flows with staging."
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  core_common:
    path: ../core_common
  core_ai:
    path: ../core_ai
  core_document:
    path: ../core_document
  core_design_system:
    path: ../core_design_system

dev_dependencies:
  flutter_test:
    sdk: flutter
