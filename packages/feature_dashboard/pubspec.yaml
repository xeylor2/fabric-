name: feature_dashboard
description: "FEBRIC Project Dashboard: recents, pinned projects, activity, system health."
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  core_common:
    path: ../core_common
  core_ai:
    path: ../core_ai
  core_assets:
    path: ../core_assets
  core_design_system:
    path: ../core_design_system

dev_dependencies:
  flutter_test:
    sdk: flutter
