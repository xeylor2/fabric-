/// Package marker for `feature_dashboard`.
///
/// Project Dashboard: recents, pinned projects, activity, system health.
///
/// Keeps the package importable and testable from day one; real
/// implementation arrives in its architecture milestone.
abstract final class FeatureDashboardPackage {
  static const String id = 'feature_dashboard';
  static const String description =
      'Project Dashboard: recents, pinned projects, activity, system health.';
}
