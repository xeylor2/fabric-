/// Package marker for `core_assets`.
///
/// Asset Intelligence engine: asset model, embedding index client, recommendation services.
///
/// Keeps the package importable and testable from day one; real
/// implementation arrives in its architecture milestone.
abstract final class CoreAssetsPackage {
  static const String id = 'core_assets';
  static const String description =
      'Asset Intelligence engine: asset model, embedding index client, recommendation services.';
}
