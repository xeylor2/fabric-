/// Package marker for `feature_repeat`.
///
/// Repeat feature: repeat configuration, tiled preview, seam tools.
///
/// Keeps the package importable and testable from day one; real
/// implementation arrives in its architecture milestone.
abstract final class FeatureRepeatPackage {
  static const String id = 'feature_repeat';
  static const String description =
      'Repeat feature: repeat configuration, tiled preview, seam tools.';
}
