name: feature_design_tree
description: "FEBRIC Design Tree panel: node operations, locks, versions, tree navigation."
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  core_common:
    path: ../core_common
  core_document:
    path: ../core_document
  core_workspace:
    path: ../core_workspace
  core_design_system:
    path: ../core_design_system

dev_dependencies:
  flutter_test:
    sdk: flutter
