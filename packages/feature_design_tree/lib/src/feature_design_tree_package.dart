/// Package marker for `feature_design_tree`.
///
/// Design Tree panel: node operations, locks, versions, tree navigation.
///
/// Keeps the package importable and testable from day one; real
/// implementation arrives in its architecture milestone.
abstract final class FeatureDesignTreePackage {
  static const String id = 'feature_design_tree';
  static const String description =
      'Design Tree panel: node operations, locks, versions, tree navigation.';
}
