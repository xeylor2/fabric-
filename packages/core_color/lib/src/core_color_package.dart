/// Package marker for `core_color`.
///
/// Color engine: palettes, colorways, ICC/CMYK transforms, gamut and delta-E analysis.
///
/// Keeps the package importable and testable from day one; real
/// implementation arrives in its architecture milestone.
abstract final class CoreColorPackage {
  static const String id = 'core_color';
  static const String description =
      'Color engine: palettes, colorways, ICC/CMYK transforms, gamut and delta-E analysis.';
}
