targets:
  $default:
    builders:
      json_serializable:
        options:
          # Nested domain models must serialize to plain JSON maps, not
          # model instances — required for lossless .febric round-trips.
          explicit_to_json: true
